# salarydata — Project Brain

> Per-repo brain, migrated from central claude-memory 2026-06-20. Canonical project memory now lives here.

## Brain

- **Aug 9 2026 (Fable directive move 1):** CTR rewrite pass, titles + meta descriptions only, on the 20 highest impression-x-CTR-gap pages. Baseline 32,037 impr / 73 clicks / **0.228% CTR** over 2026-07-11 to 2026-08-07 (whole site: 49,319 impr @ 0.29%). Root cause: 16 of the 20 were still serving the untouched template title `{Job} Salary UK 2026 | Average Pay by City & Experience`, which matches no real query, and the location hubs led with `{City} Salaries` while every query says "average salary in {city}" / "average wage in {city}". Fix mechanism: per-page `seoTitle`/`seoDescription` in `src/content/jobs/*.json` and `src/content/locations/*.json` (the templates at `src/pages/salary/[job]/index.astro:33` and `src/pages/location/[location]/index.astro:51` already fall back to these) — no template edit needed for hub pages. The one money page in scope, `/salary/software-engineer/london/`, needed a new `titleOverrides`/`descOverrides` map added at `src/pages/salary/[job]/[location]/index.astro:87` keyed `${job.slug}/${location.slug}` — **use that map for any future money-page CTR work**. Every figure in every new title comes from that page's own JSON (median/entry/senior/quartiles) or its rendered city table, nothing invented. Also removed a **false "RICS Data" sourcing claim** from quantity-surveyor (source is ONS ASHE), and stripped arrow glyphs + en dashes from the 6 pre-existing overrides. Excluded `/salary/accountant/` (pos 41.6) and `/salary/architect/` (pos 32.3) despite big impressions — below page 3 a title cannot pay. Commit `9eb20ac`, pushed, deployed by wrangler, all 20 verified live on the apex. **Gate: blended CTR > 0.6% on these 20, check 2026-08-23; revert any page that lost position. No second CTR pass on this site before 2026-09-09.** 8 out-of-scope pages still carry em dashes in their titles/metas (`/`, `/about/`, `/company/`, `/methodology/`, `/privacy/`, `/salary/`, 5 `/company/*/salaries/` descriptions) plus an arrow in `/location/cardiff/` — left alone to respect the 20-page cap.
- **Jul 20 2026:** CTR push for "seo manager salary uk" (624 impr, pos 13.2, 0% CTR) and "seo executive salary uk" (465 impr, pos 14.8, 0% CTR) — confirmed via SEO Gets GSC data that BOTH queries land on the single page `/salary/seo-manager/` (no dedicated seo-executive page exists in the dataset). Touched 4 pages total: `seo-manager.json` (seoTitle/seoDescription override — old: none/default templated title → new: `SEO Manager Salary UK 2026 | £45k Median + Career Path` / `SEO Manager salary: £45k median, entry £28k+ (SEO Executive), senior £75k+. London pay, progression path, take-home calculator. Explore →`), plus `src/pages/salary/[job]/index.astro` (shared template) gated by `job.slug === 'seo-manager'` to add: an Executive→Manager→Head of SEO progression section, a London-vs-rest-of-UK section (uses existing cityRows/locationMultiplier data), a "what affects salary" section, and 2 FAQ+FAQPage entries answering "how much does an SEO Executive earn" / "is SEO Executive the same as SEO Manager" directly — all figures pulled from `seo-manager.json`'s existing entryLevelSalary/nationalMedianSalary/seniorLevelSalary/quartiles, nothing invented. Also added a single gated cross-link paragraph (whitelist `['content-manager','marketing-manager','paid-media-manager']`, all 3 already list `seo-manager` in their own `relatedJobs`) so those 3 hub pages link into `/salary/seo-manager/` — verified via dist grep that zero other job hub pages (e.g. solicitor, brand-manager) picked up any of this. Build passed (666 pages). Committed locally, NOT pushed (deploy is manual wrangler, per Sunny's instruction not to push this session). Check GSC again after ~2-3 weeks for CTR delta on both queries.
- **Jul 10 2026:** GA4 property 538515864 was returning zero rows. Root cause: the gtag snippet (G-8QQ1H756ZY) was already correct in `src/layouts/BaseLayout.astro` and already committed to git (commits `5a9564a`, `3c5c994`), but this repo deploys via manual `wrangler pages deploy`, not on git push, so the fix had never actually shipped to Cloudflare Pages. Ran `npm run build` then `npx wrangler pages deploy dist --project-name salarydata --commit-dirty=true`; confirmed live via curl on both `https://salarydata.pages.dev` and `https://salarydata.co.uk` that the page now serves `gtag/js?id=G-8QQ1H756ZY` and `gtag('config','G-8QQ1H756ZY')`. No source changes needed, no new commit (dist/ is gitignored). Sunny still needs to mark key events in the GA4 UI once real traffic starts flowing.

## Current state

- **What:** salarydata.co.uk — UK programmatic salary site targeting "[job title] salary in [UK city]". Money via Reed/TotalJobs CPC + TopCV/CVLibrary CV-review affiliate. Differentiator: data-trust (ONS ASHE source + sample size + last-updated + method) and a signature percentile range bar.
- **Stack:** Astro 5 + React islands + TypeScript, Cloudflare Pages (CF project `salarydata`, sunnypat81 account).
- **Repo:** `sunnyp81/salarydata` (public, single-commit history), branch `main`. Local `C:\Users\sunny\repos\salarydata`.
- **Live:** apex `https://salarydata.co.uk` IS live and serving from CF Pages (confirmed by curl Aug 9 2026; the older "preview only" note is outdated). GSC records 49,319 impressions/28d on `sc-domain:salarydata.co.uk`.
- **Deploy:** 🔴 **manual wrangler only — pushing to GitHub does NOT deploy this site.** Run `npm run build` then `npx wrangler pages deploy dist --project-name salarydata --branch main --commit-dirty=true` (cached wrangler OAuth works, no token needed as of Aug 9 2026). `.github/workflows/deploy.yml` exists locally but is gitignored and **confirmed absent from the remote** (`gh api repos/sunnyp81/salarydata/contents/.github/workflows` returns 404), so the Actions pipeline has never run. Allow ~30s of CF edge cache before curl-verifying a change; some paths serve the old HTML for a minute.
- **Scale:** 664 pages built (600 money `/salary/[job]/[city]/`, 30 job hubs, 20 city hubs, 5 FTSE company pages, homepage, calculator, indexes, trust pages). Semantic audit ~91/100.

## Key facts & warnings

- 🔴 **WAVE PUBLISHING — only 30 of 600 money pages index week 1.** See `src/lib/publishConfig.ts::INDEXED_MONEY_PAGES`; the other 570 carry `<meta name="robots" content="noindex, follow">`, and the sitemap auto-filters to indexable URLs only (~95). This is deliberate: launching 666 pages day-one on a fresh domain risks the March 2026 Core Update demotion pattern that hit calculator.place. To release a wave: add `'job-slug/city-slug'` pairs to `INDEXED_MONEY_PAGES`, redeploy. Cadence +50/week from week 2, re-evaluate on GSC signals.
- 🔴 **Don't expand to the planned 200K pages until the 600-page seed shows GSC indexing + ranking.** Quality-per-page > raw count. Every new job/city must bring a category-specific narrative block, not the shared 5-tip skeleton.
- **Anti-templating:** 6 category-specific H2/narrative archetypes (Tech/Finance/Marketing/Healthcare/Legal/Construction) rotate across the 600 money pages to break the identical-H2 signal that demoted calculator.place.
- **Secrets are pointers only** — CF API token, StaticForms key, and any affiliate URLs live in the Drive-only master-builds vault, never in this brain or git. Affiliate env vars: `TOPCV_AFFILIATE_URL`, `CVLIBRARY_AFFILIATE_URL`, `REED_API_KEY` (set in CF env when approved).
- **GSC:** `sc-domain:salarydata.co.uk` added to BOTH gsc-sunnypat81 and gsc-2012infinite; verifies once DNS TXT set. Bing `https://salarydata.co.uk` added; import from GSC after Google verifies.
- **Google Indexing API:** `scripts/pushToIndexingApi.mjs` ready with 30 priority URLs; SA JSON `claudeindex-6b5681c013d4.json` was not on the machine.
- Tax engine: 2024/25 England/Wales/NI bands, PA taper >£100k, NI 8%/2%, student loan plans 1/2/4/5/PG, pension salary-sacrifice. Schema: BreadcrumbList + FAQPage + Occupation per page. Editor = Sunny Patel (Person schema, sameAs → sunnypatel.co.uk + LinkedIn) — this is REAL, not fabricated.
- **AdSense:** hold off until 5K+ monthly clicks (premature ads waste, per calculator.place lesson).
- Full data-first redesign Claude prompt + competitor research (PayScale/Glassdoor/Indeed/ONS ASHE) preserved in central `salarydata-claude-design-prompt.md`.

## History

- **Apr 22 2026:** built + deployed (664 pages). Redesign (Fraunces + Inter, navy/amber, tabular numerals, data-publication hero), launch SEO (Sunny Person/Org schema, /directory/ A-Z hub, OG PNG, AI-crawler allowances), wave-publishing system. Semantic audit climbed 62 → 84 → ~91 across P1-P3 passes.
- **Sunny TODOs to monetise:** connect apex DNS, GSC TXT verify + sitemap, add `.github/workflows/` via web UI, push sunnypatel-nextjs backlink (`b159c64` local-only), Indexing API push, apply Reed/TopCV/CVLibrary programmes, Rich Results test once apex live.
